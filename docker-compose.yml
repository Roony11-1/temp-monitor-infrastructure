services:
  temp-db:
    image: postgres:15
    container_name: temp-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-tempdb}
      POSTGRES_USER: ${POSTGRES_USER:-admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-admin}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - temp-data:/var/lib/postgresql/data
    networks:
      - temp-network

  temp-backend:
    build:
      context: ../temp-monitor-api
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://temp-db:5432/${POSTGRES_DB:-tempdb}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-admin}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-admin}
      APP_JWT_SECRET: ${APP_JWT_SECRET:?Debe definir APP_JWT_SECRET}
      APP_JWT_ACCESS_EXPIRATION_MINUTES: ${APP_JWT_ACCESS_EXPIRATION_MINUTES:-15}
      APP_JWT_REFRESH_EXPIRATION_DAYS: ${APP_JWT_REFRESH_EXPIRATION_DAYS:-7}
      CAMARA_MUESTREO_CADENCIA_MS: ${CAMARA_MUESTREO_CADENCIA_MS:-165000}
      CAMARA_MUESTREO_INITIAL_DELAY_MS: ${CAMARA_MUESTREO_INITIAL_DELAY_MS:-60000}
    ports:
      - "${SERVER_PORT:-8080}:8080"
    restart: unless-stopped
    depends_on:
      - temp-db
    networks:
      - temp-network

  temp-frontend:
    build:
      context: ../temp-monitor-frontend
      args:
        VITE_API_URL: ${VITE_API_URL:-}
        VITE_CAMARA_MUESTREO_CADENCIA_MS: ${VITE_CAMARA_MUESTREO_CADENCIA_MS:-165000}
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    restart: unless-stopped
    depends_on:
      - temp-backend
    networks:
      - temp-network

networks:
  temp-network:
    driver: bridge

volumes:
  temp-data:
